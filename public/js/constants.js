const result = document.querySelector(".list-group");
const registForm = document.querySelector("#reg_form");
const api_alert = document.querySelector(".alert_msg");
const update = document.querySelector('[name="update"]');
const register = document.querySelector('[name="register"]');

const state = {};

export { result, registForm, api_alert, update, state, register };
